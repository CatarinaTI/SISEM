#ifndef ENUMS_H_
#define ENUMS_H_

typedef enum alinea_enum {a, b, c} alinea_enum_t;
typedef enum alineaB_enum {l, p} alineaB_enum_t;
typedef enum alineaC_enum {t, h} alineaC_enum_t;

#endif /* ENUMS_H_ */
